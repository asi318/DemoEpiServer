@using DemoEpiServer
@model PageViewModel<LandingPage>
<div class="campaign-wrapper">
    @Html.PropertyFor(x => x.CurrentPage.MainContentArea, new { CssClass = "row equal-height", tag = Global.ContentAreaTags.FullWidth })
</div>
<script>
    var $myGroup = $('.accordioncontentblock');
    $myGroup.on('show.bs.collapse', '.collapse', function () {alert($myGroup.siblings().find('.collapse .in'))
        $myGroup.siblings().find('.collapse .in').collapse('hide');
    });
</script>